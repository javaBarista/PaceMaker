<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="15dp">

    <TextView
        android:layout_width="0dp"
        android:layout_height="30dp"
        android:id="@+id/qanda_title"
        android:textSize="20dp"
        android:textColor="#000000"
        android:layout_marginLeft="2dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="19dp"
        android:id="@+id/qanda_register"
        android:textSize="13dp"
        android:textColor="#666666"
        android:text="등록자 : "
        android:textStyle="bold"
        android:layout_marginLeft="2dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/qanda_title" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="19dp"
        android:id="@+id/qanda_body"
        android:textSize="14dp"
        android:textColor="#666666"
        android:layout_marginLeft="2dp"
        app:layout_constraintLeft_toRightOf="@+id/qanda_register"
        app:layout_constraintTop_toBottomOf="@id/qanda_title" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/qanda_Date"
        android:textSize="13dp"
        android:textColor="#666666"
        android:textStyle="italic"
        android:layout_marginTop="5dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginLeft="2dp"
        android:layout_marginRight="2dp"
        android:background="#FF696969"
        app:layout_constraintTop_toBottomOf="@+id/qanda_body"/>
</androidx.constraintlayout.widget.ConstraintLayout>